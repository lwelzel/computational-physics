import numpy as np
_rng = np.random.default_rng()


def get_initial_grid(n=50):
    return _rng.choice([True, False], shape=(n, n), dtype=bool)

def

def lsing():
    rng = np.random.default_rng









if __name__ == "__main__":
    lsing()
